# Fully pinned: the sidecar's behaviour depends on exact runtime versions
# (numpy 2.x-style major bumps changed behaviour silently before). `mlx` is
# listed explicitly instead of riding along as a transitive dependency.
mlx==0.31.2
mlx-embeddings==0.1.0
mlx-lm==0.31.3
mlx-vlm==0.5.0
mlx-whisper==0.4.3
mlx-qwen3-asr==0.3.5
numpy==2.4.6
onnxruntime==1.27.0
opencv-python==4.13.0.92
pillow==12.3.0
pyclipper==1.4.0
PyYAML==6.0.3
soundfile==0.13.1
huggingface-hub==1.16.1
socksio==1.0.0
